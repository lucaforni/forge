# Medium drafts — not for version control
medium/
node_modules/
install-forge.ts.bak
dev/
.forge/.backups
.forge/.install-manifest.json
# Local smoke-test credentials (see .env.example — never commit real keys)
.env
.env.local
